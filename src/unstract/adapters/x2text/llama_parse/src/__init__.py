from llama_parse import LlamaParse

metadata = {
    "name": LlamaParse.__name__,
    "version": "1.0.0",
    "adapter": LlamaParse,
    "description": "LlamaParse X2Text adapter",
    "is_active": True,
}